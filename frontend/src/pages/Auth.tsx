import React from 'react';

const Auth = () => (
  <div>
    <h2>Authentification / Authentication</h2>
    <p>Connexion, inscription, gestion du wallet, etc.</p>
    <p>Login, registration, wallet management, etc.</p>
  </div>
);

export default Auth;
