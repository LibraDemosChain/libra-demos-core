import React from 'react';

const NotFound = () => (
  <div>
    <h2>Page non trouvée / Page not found</h2>
    <p>La page demandée n’existe pas. / The requested page does not exist.</p>
  </div>
);

export default NotFound;
